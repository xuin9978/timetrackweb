我将优化 `Sidebar.tsx` 中按钮的代码，确保所有 PWA 底部导航按钮（包括用户按钮和添加按钮）在未选中/默认状态下都统一为**灰色**风格，以保持视觉一致性。

**执行计划:**

1.  **修改 `components/Sidebar.tsx`**:
    *   **清理用户按钮代码**: 将之前修改的 `isLoggedIn ? ... : ...` 三元表达式简化，直接使用统一的灰色样式类名 (`bg-gray-100 text-gray-400...`)，因为现在无论登录与否都要求显示为灰色。
    *   **确认添加按钮样式**: 确保添加按钮 (`onAddEvent`) 也使用相同的灰色样式 (`bg-gray-100 text-gray-400`)，以响应"PWA下的 button...也是灰色"的描述。

这不仅精简了代码，也再次确认了您对灰色统一风格的要求。