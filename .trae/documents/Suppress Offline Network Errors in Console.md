我将通过修改 `index.tsx` 来屏蔽 `net::ERR_INTERNET_DISCONNECTED` 等网络断开错误，防止它们刷屏控制台，同时保持应用内部的离线检测逻辑正常运行。

**执行计划:**

1.  **修改 `index.tsx`**:
    *   在全局错误监听器中增加对 `ERR_INTERNET_DISCONNECTED`、`Failed to fetch` 等网络相关错误信息的过滤。
    *   当捕获到这些特定的网络错误时，阻止其在控制台中输出（`preventDefault` 或不打印），从而保持控制台整洁。

2.  **确认 `App.tsx` 逻辑**:
    *   `App.tsx` 中已经具备了完善的错误捕获逻辑（第 552-561 行），能够识别这些错误并将应用状态设置为“离线模式”（显示“当前离线”提示）。
    *   因此，屏蔽控制台的红色报错日志不会影响用户界面的正常反馈。

这个操作旨在消除开发预览时的噪音，让日志更专注于业务逻辑。