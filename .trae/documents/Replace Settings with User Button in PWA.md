我将修改 `index.html` 中的 CSS 样式，在 PWA 底部导航栏中隐藏"设置"按钮并显示"用户"按钮，从而实现图标的替换。

**执行计划:**

1. **更新** **`index.html`**:

   * 在 `@media (max-width: 768px)` 样式块中，找到当前隐藏用户按钮的 CSS 规则（`.sidebar-container > div:last-child > button:first-child...`）。

   * 将其替换为新的规则：

     * **显示** 用户按钮（即容器内的第一个按钮）。

     * **隐藏** 设置按钮（即容器内的最后一个按钮）。

   * 这样，在手机端（PWA 模式）的底部导航栏最右侧将显示"用户"头像，点击可管理账号；而桌面端布局保持不变，依然同时显示两个按钮。

