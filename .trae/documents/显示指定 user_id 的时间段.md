## 目标复述
- 让邮箱 `x50134397@163.com`、`user_id=47cdb2cb-1753-480e-8452-25f7ab09b64d` 的所有时间段在前端正常显示；如当前项目数据为空或散落在旧项目，需将数据“恢复/迁移回”当前项目并显示。

## 恢复策略
- A. 显示修复（优先）：
  - 校验 `.env.local` 指向你提供的 Supabase 项目；确认登录后 `session.user.id` 等于 `47cdb2cb...`。
  - 按视图区间（天/周/月）查询并显示事件；对跨日边界使用并集查询避免漏查；验证后关闭调试开关保持正常范围。
- B. 云端已有但未显示：
  - 若控制台查询到该 `user_id` 的记录，前端仍不显示，则以诊断日志比对区间和分类可见性，修复查询与可见标签合并逻辑后刷新显示。
- C. 当前项目空表需恢复：提供两种恢复路径（可并行）：
  1) 文件导入：通过应用“设置 → 数据恢复/导入”，导入 `ICS/JSON`（应用已支持），再用 `replaceAllEvents(user_id, items)` 覆盖写入云端。
  2) 跨项目迁移脚本：提供旧项目的 `URL/KEY`，脚本读取旧项目 `events/tags` 中 `user_id=47cdb2cb...` 的数据，清洗并写入到新项目，所有插入均设置 `user_id=47cdb2cb...`；完成后刷新并显示。

## 验证
- 登录后右上角显示邮箱；控制台诊断打印 `user_id`、视图区间与返回条数；页面出现对应时间段；抽样核对几条记录（标题、起止时间、分类）。

## 风险与注意
- RLS：仅 `auth.uid() = user_id` 可读写；迁移脚本需要使用该用户的上下文或用服务角色（仅在安全环境）执行。
- 时区：数据库存 UTC，前端显示为本地时间；跨日事件在天/周视图边界最敏感，需并集查询与本地化转换。

## 执行项（确认后立刻实施）
- 增强并集查询与诊断日志；
- 开启并使用导入/恢复功能（ICS/JSON）；
- 若需，从旧项目拉取并迁移到新项目；
- 端到端验证并提供预览链接。

最新预览： http://localhost:3000/