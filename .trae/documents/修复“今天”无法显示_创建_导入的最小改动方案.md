## 发现与判断
- 事件渲染使用 `isSameDay(event.date, day)`，本地时区判断正确；“今天”样式基于 `isSameDay(date, new Date())`。
- 创建交互容易被误判为水平滚动（5px 阈值），或被事件块拦截；点击吸附阈值 30 分钟过大影响创建起点。
- AddEventModal 未校验时间顺序，可能产生 0 分钟或负时长记录。
- Supabase 本地↔UTC 转换与本地日归一正常；ICS 导入完成后会 union 分类到 `visibleTags`，不会被误过滤。

## 修改项（最小改动）
1) 减少误判滚动：
- 文件：`components/TimeGrid.tsx`
- 把水平滚动触发阈值从 `5px` 提升到 `10-12px`：`if (Math.abs(dx) > 12) { setIsScrolling(true); ... }`

2) 降低点击吸附强度：
- 文件：`components/TimeGrid.tsx`
- 将 `SNAP_THRESHOLD` 从 `30` 调整为 `10-15`，避免点击靠近前一事件结束时被强制贴齐。

3) 提交前校验时间顺序：
- 文件：`components/AddEventModal.tsx`
- 在 `handleSubmit` 中对 `startTime/endTime` 做分钟比较；若 `end <= start`，自动把 `end` 调整为 `start + 15min`（用 `date-fns` 的 `addMinutes/format`）。

4) 可选：全天 ICS 语义更准确（非必须）
- 文件：`utils/dateUtils.ts` `parseICS`
- 对 `YYYYMMDD` 的全天事件设置为 `00:00-23:59`（当前默认 9:00 起，可保留现状）。

## 验证用例
- 未登录与已登录两种状态下，在“今天”创建普通与跨日 (`23:00-02:00`)；确保今天/次日都正确显示。
- 在“今天”导入包含今天与跨日的 ICS；确认今天片段显示且分类可见。
- 重复创建靠近上一事件结束，确认不被过度吸附；横向轻微移动不再误判为滚动。

## 预期影响
- 仅交互门槛与输入校验变更，不影响现有时区与按日过滤；不会对非“今天”的日期造成副作用。

请确认，我将按上述四项在对应文件提交最小改动，并进行本地验证。