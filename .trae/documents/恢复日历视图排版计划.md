# 回滚日历视图排版计划

根据您的反馈，新版日历视图的排版（特别是 Header 布局和对齐方式）导致了预期之外的变动。我将把日历视图的核心排版恢复到之前的状态，保留底部导航栏的 PWA 适配。

## 1. 恢复 App 容器对齐方式 (`App.tsx`)
- 将根容器的对齐方式从 `items-start` 恢复为 `items-center`。
- 恢复内边距 `p-2`。
- 这将确保页面内容在屏幕中间垂直居中，恢复之前的视觉重心。

## 2. 恢复日历头部与面板布局 (`Calendar.tsx`)
- **Header**：恢复标题的 **绝对定位** (`absolute top-6 left-1/2`)，确保它始终在视觉上居中，不受右侧按钮组影响。
- **EventPanel**：恢复之前的侧边滑出模式，移除移动端的底部抽屉（Drawer）样式，确保交互逻辑与之前完全一致。
- **字体与间距**：恢复之前的字体大小和间距设置。

## 3. 验证
- 重启服务器。
- 确认日历视图完全恢复到修改前的样子（标题居中，侧边栏逻辑恢复）。
- 确认底部导航栏（Sidebar）依然保持 PWA 适配状态（因为这是在 `index.html` 中定义的，本次不回滚，除非您要求）。
