## 问题
- 通过 .ics 导入的事件在云端不可用时未写入本地缓存，重登后丢失。

## 改动点
- 更新 `App.tsx:handleImportEvents`（约 319-365 行）：
  - 维持现有去重 `isDup` 与 `uniqueEvents`。
  - 登录态：`try { createEvents(...) }`；若返回空或异常，`setEvents(prev => [...prev, ...uniqueEvents])` 并提示“云端未保存，已暂存到本地”。
  - 未登录态：仍本地写入。
- 保持现有登录后合并逻辑（`App.tsx:301-321`）：
  - 已将本地事件 `date` 标准化为 `Date`，并将 `missing` 与云端 `fetchedEvents` 合并；无需额外改动。

## 验证
- 登录→通过“导入日历文件”导入 .ics 并选择标签→立即显示事件。
- 退出→重登：事件仍显示；在云端可用时，自动补齐上报，之后能在 Supabase 后台看到记录。

## 影响范围
- 仅修改导入流程的容错与回退；不变更 UI 与现有 CRUD 结构。

## 风险控制
- 继续使用 `isDup` 去重，避免重复渲染。
- 失败提示与本地回退确保数据不丢失。